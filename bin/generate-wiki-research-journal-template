#!/usr/bin/python
import sys
import datetime

TEMPLATE = """# {title}

"""

title = (datetime.date.today() if len(sys.argv) < 2
        # Expecting filename in <title>.foo format
        else sys.argv[1].rsplit(".", 1)[0])
print(TEMPLATE.format(title=title))
